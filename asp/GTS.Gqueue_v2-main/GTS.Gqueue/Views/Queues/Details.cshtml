@model GTS.Gqueue.Entities.Queue

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Queue</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-controller="AwaitingPeople" asp-action="Index" asp-route-queueId="@Model.Id">Show Awaiting People</a> |
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to Queues</a>
</div>
